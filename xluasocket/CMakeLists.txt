project(xluasocket)
SET(SRC_LIST ./lxluasocket.c ./ringbuf.c write_buffer.c)
message(STATUS ${SRC_LIST})
ADD_DEFINITIONS(-DLUA_BUILD_AS_DLL)
include_directories(.)
set(LIBRARY_OUTPUT_PATH ../../bin)
add_library(xluasocket SHARED ${SRC_LIST})

project(ringbuf_unittest)
set(SRC_LIST ./ringbuf_unittest.cc ./ringbuf.c ./write_buffer.c)
message(STATUS ${SRC_LIST})
include_directories(.)
include_directories(../googletest/include)
set(EXECUTABLE_OUTPUT_PATH ../../bin)
add_executable(ringbuf_unittest ${SRC_LIST})
