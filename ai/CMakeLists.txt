project(ai)
file(GLOB AI_H *.h)
aux_source_directory(. AI_CPP)
message(STATUS ${AI_CPP})
include_directories(../src)
ADD_DEFINITIONS(-DLUA_BUILD_AS_DLL)
SET(LIBRARY_OUTPUT_PATH ../../bin)
add_library(ai SHARED ${AI_H} ${AI_CPP})
link_directories(../../bin/Debug)
target_link_libraries(ai liblua)